services:
  postgres:
    image: postgres:16-alpine
    container_name: vestya-db
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: vestya
      POSTGRES_USER: vestya
      POSTGRES_PASSWORD: vestya_dev
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U vestya"]
      interval: 5s
      timeout: 5s
      retries: 5

  minio:
    image: minio/minio:latest
    container_name: vestya-storage
    ports:
      - "9000:9000"
      - "9001:9001"
    environment:
      MINIO_ROOT_USER: minioadmin
      MINIO_ROOT_PASSWORD: minioadmin
    command: server /data --console-address ":9001"
    volumes:
      - minio_data:/data

volumes:
  postgres_data:
  minio_data:
